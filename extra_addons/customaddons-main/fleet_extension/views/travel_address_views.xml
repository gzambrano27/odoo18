<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- =============================
         🔹 Vista Lista (Odoo 18 usa <list>)
    ============================== -->
    <record id="view_travel_address_list" model="ir.ui.view">
        <field name="name">travel.address.list</field>
        <field name="model">travel.address</field>
        <field name="arch" type="xml">
            <list string="Direcciones">
                <field name="name"/>
                <field name="place"/>
            </list>
        </field>
    </record>

    <!-- =============================
         🔹 Vista Formulario
    ============================== -->
    <record id="view_travel_address_form" model="ir.ui.view">
        <field name="name">travel.address.form</field>
        <field name="model">travel.address</field>
        <field name="arch" type="xml">
            <form string="Dirección">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="place"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>