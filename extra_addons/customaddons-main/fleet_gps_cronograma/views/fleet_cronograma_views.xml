<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================= VISTA CALENDARIO ================= -->
    <record id="view_fleet_cronograma_calendar" model="ir.ui.view">
        <field name="name">fleet.cronograma.calendar</field>
        <field name="model">fleet.cronograma</field>
        <field name="arch" type="xml">
            <calendar
                    string="Cronograma de Maniobras"
                    date_start="necesidad_entrega"
                    date_stop="date_stop2"
                    mode="month"
                    color="color">
                <field name="sequence"/>
                <field name="maniobra" string="Requerimiento"/>
                <field name="tipo_ids" widget="many2many_tags"/>
                <field name="conductor_id"/>
                <field name="vehicle_display"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'pending'"
                       decoration-muted="state == 'draft'"
                       decoration-danger="state == 'rechazado'"
                       decoration-success="state == 'aprobado'"
                       decoration-warning="state == 'finalizado'"/>
            </calendar>
        </field>
    </record>

    <!-- ================= VISTA CALENDARIO (HEREDADA) ================= -->
    <record id="view_fleet_cronograma_calendar_override" model="ir.ui.view">
        <field name="name">fleet.cronograma.calendar.override</field>
        <field name="model">fleet.cronograma</field>
        <field name="inherit_id" ref="fleet_gps_cronograma.view_fleet_cronograma_calendar"/>
        <field name="arch" type="xml">
            <xpath expr="//calendar" position="attributes">
                <attribute name="date_start">start_readonly</attribute>
                <attribute name="date_stop">date_stop2</attribute>
            </xpath>
        </field>
    </record>

    <!-- ================= VISTA LISTA ================= -->
    <record id="view_fleet_cronograma_list" model="ir.ui.view">
        <field name="name">fleet.cronograma.list</field>
        <field name="model">fleet.cronograma</field>
        <field name="arch" type="xml">
            <list string="Cronograma de Maniobras">
                <header>
                    <button string="Imprimir Cronograma"
                            type="action"
                            name="%(action_cronograma_print_wizard)d"
                            class="btn-primary"/>
                </header>
                <field name="sequence"/>
                <field name="maniobra"/>
                <field name="tipo_ids" widget="many2many_tags"/>
                <field name="necesidad_entrega"/>
                <field name="conductor_id"/>
                <field name="fecha_finalizacion"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'pending'"
                       decoration-muted="state == 'draft'"
                       decoration-danger="state == 'rejected'"
                       decoration-success="state == 'approved'"
                       decoration-warning="state == 'finalizado'"/>
            </list>
        </field>
    </record>

    <!-- ================= VISTA FORMULARIO ================= -->
    <record id="view_fleet_cronograma_form" model="ir.ui.view">
        <field name="name">fleet.cronograma.form</field>
        <field name="model">fleet.cronograma</field>
        <field name="arch" type="xml">
            <form string="Cronograma">
                <header>
                    <!-- Botones de acción -->
                    <button name="action_send" string="Enviar" type="object"
                            invisible="state != 'draft'"
                            class="btn-primary"
                            groups="fleet_gps_cronograma.group_cronograma_usuario"/>
                    <button name="action_approve" string="Aprobar" type="object"
                            invisible="state != 'pending'"
                            class="btn-success"
                            groups="fleet_gps_cronograma.group_cronograma_admin"/>
                    <button name="action_reject" string="Rechazar" type="object"
                            invisible="state != 'pending'"
                            class="btn-danger"
                            groups="fleet_gps_cronograma.group_cronograma_admin"/>
                    <button name="action_finalize" string="Finalizar" type="object"
                            invisible="state != 'approved'"
                            class="btn-secondary"
                            groups="fleet_gps_cronograma.group_cronograma_admin"/>

                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,pending,approved,rejected,finalizado"/>
                </header>

                <sheet>
                    <group>
                        <field name="sequence" readonly="1"/>
                        <field name="maniobra" readonly="state in ('approved','rejected','finalizado')"/>
                        <field name="tipo_ids" widget="many2many_tags"
                               readonly="state in ('approved','rejected','finalizado')"/>
                        <field name="necesidad_entrega" readonly="state in ('approved','rejected','finalizado')"/>
                        <field name="numero_dias" readonly="state in ('approved','rejected','finalizado')"/>
                    </group>

                    <group string="Logística">
                        <field name="proveedor_externo" readonly="state in ('rejected','finalizado')"/>
                        <field name="vehiculo_id"
                               invisible="proveedor_externo == True"
                               readonly="state in ('rejected','finalizado')"/>
                        <field name="vehicle_ref"
                               invisible="proveedor_externo == False"
                               readonly="state in ('rejected','finalizado')"/>
                        <field name="conductor_id" readonly="state in ('rejected','finalizado')"/>
                        <field name="origen" readonly="state in ('rejected','finalizado')"/>
                        <field name="destino" readonly="state in ('rejected','finalizado')"/>
                        <field name="peso_carga" readonly="state in ('rejected','finalizado')"/>
                        <field name="unidad_peso" readonly="state in ('rejected','finalizado')"/>
                        <field name="fecha_ejecucion" readonly="state in ('rejected','finalizado')"/>
                        <field name="fecha_finalizacion"
                               readonly="1"
                               invisible="state != 'finalizado'"/>
                    </group>

                    <group groups="fleet_gps_cronograma.group_cronograma_admin">
                        <field name="entrega" readonly="state in ('rejected','finalizado')"/>
                        <field name="observacion"
                               invisible="entrega != 'parcial'"
                               readonly="observacion != False"/>
                    </group>
                </sheet>

                <div class="oe_chatter sidebar">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ================= ACCIÓN Y MENÚ ================= -->
    <record id="action_fleet_cronograma" model="ir.actions.act_window">
        <field name="name">Cronograma</field>
        <field name="res_model">fleet.cronograma</field>
        <field name="view_mode">list,form,calendar</field>
        <field name="context">{}</field>
    </record>

    <menuitem id="fleet_cronograma_menu_root"
              name="Cronograma"
              parent="fleet.menu_root"
              sequence="20"
              groups="fleet_gps_cronograma.group_cronograma_usuario"/>

    <menuitem id="fleet_cronograma_menu"
              name="Cronograma de Maniobras"
              parent="fleet_cronograma_menu_root"
              action="action_fleet_cronograma"
              sequence="1"
              groups="fleet_gps_cronograma.group_cronograma_usuario"/>
</odoo>
